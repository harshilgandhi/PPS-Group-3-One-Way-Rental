<project name="Deliverable_2" default="default" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="src" location="src/rental/g3" />
  <property name="build" location="../rental_sim/rental/g3" />

  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>
  
  <target name="default"
      depends="copy"
	/>


  <target name="copy">
	  <copy todir="${build}">
	    <fileset dir="${src}"/>
	  </copy>
  </target>

  <target name="clean"
        description="clean up" >
    <delete dir="${build}"/>
  </target>
</project>